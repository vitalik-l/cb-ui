import { styled } from '@cb-general/core/utils/styled';
import { Tabs } from '@cb-general/weekend/Tabs';
// local files
import styles from './GeTabs.module.scss';

export const GeTabs = styled(Tabs, styles);
