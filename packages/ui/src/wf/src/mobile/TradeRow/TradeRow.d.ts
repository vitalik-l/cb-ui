import React from 'react';
declare type Props = {
    className?: string;
    noDataLabel?: string;
    children?: React.ReactNode;
};
export declare const TradeRow: {
    (props: Props): JSX.Element;
    defaultProps: {
        noDataLabel: string;
    };
};
export {};
