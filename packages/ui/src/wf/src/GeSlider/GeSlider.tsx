import { styled } from '@cb-general/core/utils/styled';
import { Slider } from '@cb-general/weekend/Slider';
// local files
import styles from './GeSlider.module.scss';

export const GeSlider = styled(Slider, styles);
