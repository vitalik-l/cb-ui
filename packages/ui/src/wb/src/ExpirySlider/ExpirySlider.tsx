import { Slider as CoreSlider } from '@cb-general/core/Slider';
import { styled } from '@cb-general/core/utils/styled';
import styles from './WbExpirySlider.module.scss';

export const ExpirySlider = styled(CoreSlider, styles);
