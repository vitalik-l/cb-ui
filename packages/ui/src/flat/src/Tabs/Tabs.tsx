import { Tabs as CoreTabs } from '@cb-general/core/Tabs';
import { styled } from '@cb-general/core/utils/styled';
import styles from './FlatTabs.module.scss';

export const Tabs = styled(CoreTabs, styles);
