import { styled } from '@cb-general/core/utils/styled';
import { ButtonBase } from '@cb-general/core/ButtonBase';
// local files
import styles from './MenuItemButton.module.scss';

export const MenuItemButton = styled(ButtonBase, styles);
