import { styled } from '@cb-general/core/utils/styled';
import { ButtonGroup } from '@cb-general/core/ButtonGroup';
// local files
import styles from './SymbolButtons.module.scss';
export const SymbolButtons = styled(ButtonGroup, styles);
