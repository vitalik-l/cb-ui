import { ValueLabel as CoreValueLabel } from '@cb-general/core/Slider';
import { styled } from '@cb-general/core/utils/styled';

// local files
import styles from './WkdValueLabel.module.scss';

export const ValueLabel = styled(CoreValueLabel, styles);
