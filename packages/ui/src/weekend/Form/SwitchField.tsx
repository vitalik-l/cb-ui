import React from 'react';

// local files
import { Switch } from '../Switch';
import { FormField } from './FormField';

export const SwitchField = (props: any) => <FormField component={Switch} {...props} />;
