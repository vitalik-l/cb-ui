import { styled } from '@cb-general/core/utils/styled';
// local
import { Button } from './Button';
import styles from './BubbleButton.module.scss';

export const BubbleButton = styled(Button, styles);
