// local files
import { createContainer } from '@cb-general/core/utils/createContainer';
import classes from '../styles/classes.module.scss';
import './Logo.scss';

export const Logo = createContainer(classes.Logo);
